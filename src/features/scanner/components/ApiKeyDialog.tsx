import React from "react";

// This component is no longer needed as we're using Supabase Edge Functions
// to proxy API requests securely. Keeping the file as a placeholder
// in case we need to restore it or implement something similar in the future.

const ApiKeyDialog: React.FC = () => {
  return null;
};

export default ApiKeyDialog;
